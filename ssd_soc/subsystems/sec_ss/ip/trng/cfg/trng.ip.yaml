# IP metadata — IPLM-lite schema v1
name: trng
version: 1.0.0           # first synthesizable / verified release
status: alpha            # proto | alpha | qual | gold
owner: @ssd-soc/security-team
subsystem: sec_ss
description: |
  True Random Number Generator (entropy source emulator + repetition
  health test + 16-entry FIFO + APB SFR + edge-latched interrupt).
bus: apb
parameters:
  FIFO_DEPTH: 16
dependencies:
  - common-libs >= 1.0.0
license: Apache-2.0
language: SystemVerilog
qual:
  lint: pending
  cdc: pending
  coverage: pending
  qual_report: null
files:
  rtl:
    - rtl/trng.sv
  sim:
    - sim/tb_trng.sv
  doc:
    - doc/DESIGN.md
    - doc/PROGRAMMERS_GUIDE.md
    - doc/trng.ipxact.xml
    - doc/diagrams/
  sw:
    - sw/trng_hal.h
    - sw/trng_hal.c
    - sw/test_hal_host.c
    - sw/Makefile
  verif:
    - verif/scenarios.yaml
