# IP metadata — IPLM-lite schema v1
# 본 파일이 IP의 single source of truth. CODEOWNERS, manifest, CI가 모두 참조.
name: nand_ctrl
version: 0.1.0           # semver. CI가 PR마다 bump 여부 확인
status: proto            # proto | alpha | qual | gold
owner: @ssd-soc/flash-team
subsystem: fcc_ss
description: |
  NAND command sequencer + timing engine
bus: axi               # axi | apb | ahb
parameters:
  CHANNELS: 8
  CE_PER_CH: 4
dependencies:
  - common-libs >= 1.0.0
license: Apache-2.0
language: SystemVerilog
qual:
  lint: pending
  cdc: pending
  coverage: pending
  qual_report: null
files:
  rtl:
    - rtl/nand_ctrl.sv
  sim:
    - sim/tb_nand_ctrl.sv
